AEGIS/
mike/
.claude/
# OS / editor
.DS_Store
**/.DS_Store

# Local dev-server logs (start.sh "without Docker" path writes these to repo root)
*.log

# Secrets & environment (never commit)
backend/.env
backend/.env.*
!backend/.env.example
backend/secrets/
**/*.pem
**/*.key

# Docker stack env (real file is local-only; the .example is committed)
.env.docker
.env.docker.local
!.env.docker.example

# Python virtualenvs (never commit)
env/
venv/
.venv/
**/__pycache__/
**/*.py[cod]

# Backend caches / local data
backend/.local-contract-storage/
backend/__pycache__/
backend/**/*.py[cod]
backend/.pytest_cache/
backend/.ruff_cache/
backend/.venv/
backend/build/
backend/dist/
backend/*.egg-info/

# Frontend build / deps
frontend/node_modules/
frontend/.next/
frontend/.turbo/
frontend/*.tsbuildinfo
frontend/.env.local
frontend/.env.production
