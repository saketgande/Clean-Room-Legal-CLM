/* Function file for ribbon commands.
   The ribbon button uses a ShowTaskpane action, which needs no code here, but
   Office requires a valid FunctionFile to be present and loaded. */
/* global Office */

Office.onReady(() => {});
